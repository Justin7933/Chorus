<div class="container mt-5">
<div class="card box-shadow">
    <div class='card-header' id='postHeader'>
        <h4>"{{title}}"</h4>
    </div>
    <div class="card-body">
        <p class="card-body">{{content}}</p>
        <div class="d-flex justify-content-between align-items-center">
            <div class="btn-group">
                <a class="link" href="/post/{{id}}">
                    <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                </a>
            </div>
            <small class="text-muted"><i>Posted by {{user.username}} on {{format_date created_at}}</i>,
                No.{{id}}</small>
        </div>
    </div>
</div>
</div>



<!--    <div class="card" style="width: 50rem">
        <div class="card-header">
            <h3><a class="link" href="/post/{{id}}">{{title}}</a></h3>
            <p>Posted by {{user.username}} on {{format_date created_at}}</p>
        </div>
        <p class="card-body">{{content}}</p>
    </div>
    !-->